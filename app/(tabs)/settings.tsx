import { PlaceholderScreen } from "../../components/ui/PlaceholderScreen";

export default function Settings() {
  return (
    <PlaceholderScreen
      section="Settings"
      headline="Settings are coming"
      description="Account, notifications and language. Round settings stay on Home, next to Play."
    />
  );
}
