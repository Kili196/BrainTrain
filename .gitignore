# ------------------------------------------------------------
# Node / package managers
# ------------------------------------------------------------
node_modules/
npm-debug.*
yarn-debug.*
yarn-error.*
.pnpm-debug.log*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ------------------------------------------------------------
# Expo
# ------------------------------------------------------------
.expo/
dist/
web-build/
expo-env.d.ts

# ------------------------------------------------------------
# Native (only present if you prebuild / eject — keep ignored in managed workflow)
# ------------------------------------------------------------
/ios/
/android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# ------------------------------------------------------------
# EAS / Expo build artifacts
# ------------------------------------------------------------
*.ipa
*.apk
*.aab

# ------------------------------------------------------------
# Environment & secrets  (Supabase keys, service_role, etc.)
# ------------------------------------------------------------
.env
.env.*
!.env.example

# ------------------------------------------------------------
# Metro
# ------------------------------------------------------------
.metro-health-check*
.metro-cache/

# ------------------------------------------------------------
# TypeScript
# ------------------------------------------------------------
*.tsbuildinfo

# ------------------------------------------------------------
# Testing / coverage
# ------------------------------------------------------------
coverage/

# ------------------------------------------------------------
# Logs & caches
# ------------------------------------------------------------
*.log
.cache/

# ------------------------------------------------------------
# OS files
# ------------------------------------------------------------
.DS_Store
Thumbs.db

# ------------------------------------------------------------
# Editors / IDEs
# ------------------------------------------------------------
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# ------------------------------------------------------------
# Local Claude Code settings (keep shared config, ignore personal overrides)
# ------------------------------------------------------------
.claude/settings.local.json

# ------------------------------------------------------------
# Design mockups (the .dc.html screens + their specs)
#
# Kept on disk so a screen can be rebuilt against its own source instead of a
# screenshot, but not committed: 4.7 MB of handoff export, none of which the app
# imports. Ask Fabian for the latest handoff zip if the folder is missing.
# ------------------------------------------------------------
mockups/
